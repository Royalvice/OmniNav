# @package _global_
defaults:
  - /robot: go2
  - /sensor: default
  - /locomotion: kinematic_gait
  - /algorithm: inspection
  - /task: inspection
  - /scene: empty
  - _self_

simulation:
  dt: 0.01
  substeps: 2
  backend: gpu
  show_viewer: true

demo:
  mode: algorithm

# Inspection pipeline currently consumes lidar-based obstacle data.
sensor:
  depth_camera: null
