# Empty scene configuration (ground only)
# @package _group_

type: empty
name: empty_scene

# Ground
ground:
  type: plane
  size: [50.0, 50.0]
  position: [0.0, 0.0, 0.0]

# No additional objects
obstacles: []
