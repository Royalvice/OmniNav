# Default sensor configuration
# @package _group_

# Depth Camera
depth_camera:
  type: depth_camera
  width: 640
  height: 480
  fov: 87  # degrees
  near: 0.1
  far: 10.0
  outputs:
    - rgb
    - depth

# 2D LiDAR
lidar_2d:
  type: lidar_2d
  num_rays: 360
  min_angle: -3.14159  # -π
  max_angle: 3.14159   # π
  min_range: 0.1
  max_range: 12.0
  height: 0.0  # Scan plane height offset
